# Codex prerequisites

- Install Codex CLI so `codex` is on your PATH.
- Authenticate Codex before running T3 Delta (for example via API key or ChatGPT auth supported by Codex).
- T3 Delta starts the server via `codex app-server` per session.
